diff --git a/node_modules/@faststore/core/cms/faststore/sections.json b/node_modules/@faststore/core/cms/faststore/sections.json
index f2b11d5..5ad2ebe 100644
--- a/node_modules/@faststore/core/cms/faststore/sections.json
+++ b/node_modules/@faststore/core/cms/faststore/sections.json
@@ -2195,6 +2195,12 @@
             }
           }
         },
+        "priceLabel": {
+          "title": "Price Label",
+          "description": "Label shown above the price on the product page",
+          "type": "string",
+          "default": ""
+        },
         "loadingLabel": {
           "title": "Loading Label",
           "type": "string",
diff --git a/node_modules/@faststore/core/src/components/sections/ProductDetails/ProductDetails.tsx b/node_modules/@faststore/core/src/components/sections/ProductDetails/ProductDetails.tsx
index 82cbbfe..e91458b 100644
--- a/node_modules/@faststore/core/src/components/sections/ProductDetails/ProductDetails.tsx
+++ b/node_modules/@faststore/core/src/components/sections/ProductDetails/ProductDetails.tsx
@@ -1,4 +1,5 @@
 import { useEffect, useMemo, useState } from 'react'
+import { PriceDisplayContext } from 'src/customizations/src/contexts/PriceDisplayContext'
 
 import type { CurrencyCode, ViewItemEvent } from '@faststore/sdk'
 
@@ -73,6 +74,7 @@ export interface ProductDetailsProps {
     usePriceWithTaxes?: boolean
     taxesLabel?: string
   }
+  priceLabel?: string
   skuMatrix?: {
     shouldDisplaySKUMatrix?: boolean
     triggerButtonLabel: string
@@ -113,6 +115,7 @@ function ProductDetails({
   notAvailableButton: { title: notAvailableButtonTitle },
   quantitySelector,
   taxesConfiguration,
+  priceLabel = '',
 }: ProductDetailsProps) {
   const {
     DiscountBadge,
@@ -200,6 +203,7 @@ function ProductDetails({
   )
 
   return (
+    <PriceDisplayContext.Provider value={{ priceLabel }}>
     <Section className={`${styles.section} section-product-details`}>
       <section data-fs-product-details>
         <section data-fs-product-details-body data-fs-content="product-details">
@@ -355,6 +359,7 @@ function ProductDetails({
         </section>
       </section>
     </Section>
+    </PriceDisplayContext.Provider>
   )
 }
 
diff --git a/node_modules/@faststore/core/src/components/sections/ProductTiles/ProductTiles.tsx b/node_modules/@faststore/core/src/components/sections/ProductTiles/ProductTiles.tsx
index 6b1d70f..fa44817 100644
--- a/node_modules/@faststore/core/src/components/sections/ProductTiles/ProductTiles.tsx
+++ b/node_modules/@faststore/core/src/components/sections/ProductTiles/ProductTiles.tsx
@@ -78,7 +78,7 @@ const ProductTiles = ({
     ),
   })
   const products = data?.search?.products
-  const productEdges = products?.edges ?? []
+  const productEdges = (products?.edges ?? []).slice(0, 4)
 
   const { sendViewItemListEvent } = useViewItemListEvent({
     products: productEdges,
@@ -95,7 +95,7 @@ const ProductTiles = ({
     }
   }, [inView, productEdges.length, sendViewItemListEvent])
 
-  if (products?.edges.length === 0) {
+  if (products && productEdges.length < 2) {
     return null
   }
 
diff --git a/node_modules/@faststore/core/src/pages/s.tsx b/node_modules/@faststore/core/src/pages/s.tsx
new file mode 100644
index 0000000..e5a4343
--- /dev/null
+++ b/node_modules/@faststore/core/src/pages/s.tsx
@@ -0,0 +1,183 @@
+import { NextSeo } from 'next-seo'
+import { useRouter } from 'next/router'
+import { useEffect, useMemo, useState } from 'react'
+
+import type { SearchState } from '@faststore/sdk'
+import type { SearchSettings } from 'src/server/cms'
+import {
+  formatSearchState,
+  parseSearchState,
+  SearchProvider,
+} from '@faststore/sdk'
+import { SROnly as UISROnly } from '@faststore/ui'
+
+import { ITEMS_PER_PAGE } from 'src/constants'
+import { useApplySearchState } from 'src/sdk/search/state'
+
+import storeConfig from 'discovery.config'
+
+import { SearchWrapper } from 'src/components/templates/SearchPage'
+import {
+  getStaticProps,
+  type SearchPageProps,
+} from 'src/experimental/searchServerSideFunctions'
+import { getStoreURL } from 'src/sdk/localization/useLocalizationConfig'
+
+export interface SearchPageContextType {
+  title: string
+  searchTerm?: string
+}
+
+const useSearchParams = ({
+  sort: defaultSort,
+}: {
+  sort: SearchState['sort']
+}) => {
+  const { asPath } = useRouter()
+
+  return useMemo(() => {
+    const url = new URL(asPath, 'http://localhost')
+
+    const shouldUpdateDefaultSort = defaultSort && !url.searchParams.has('sort')
+    if (shouldUpdateDefaultSort) {
+      url.searchParams.set('sort', defaultSort)
+    }
+
+    const newState = parseSearchState(url)
+    const hrefState = formatSearchState(newState).href
+    return parseSearchState(new URL(hrefState))
+  }, [asPath, defaultSort])
+}
+
+type StoreConfig = typeof storeConfig
+
+function generateSEOData(
+  storeConfig: StoreConfig,
+  searchTerm?: string,
+  pageSeoSettings?: SearchSettings['settings']['seo']
+) {
+  const { search: searchSeo, ...seo } = storeConfig.seo
+
+  const isSSREnabled = storeConfig.experimental.enableSearchSSR
+
+  const title = searchTerm ?? seo.title ?? 'Search Results'
+  const titleTemplate =
+    pageSeoSettings?.titleTemplate ??
+    searchSeo?.titleTemplate ??
+    seo.titleTemplate
+  const description = searchSeo?.descriptionTemplate
+    ? searchSeo.descriptionTemplate
+        .replace(/%s/g, () => searchTerm ?? '')
+        ?.trim()
+    : seo.description?.trim()
+
+  // default behavior without SSR
+  if (!isSSREnabled) {
+    return {
+      noindex: searchSeo?.noIndex ?? true,
+      nofollow: searchSeo?.noFollow ?? true,
+      title,
+      titleTemplate,
+      description,
+      openGraph: {
+        type: 'website',
+        title,
+        description,
+      },
+    }
+  }
+
+  const canonical = searchTerm
+    ? `${getStoreURL()}/s?q=${searchTerm.replaceAll(' ', '+')}`
+    : undefined
+
+  return {
+    noindex: searchSeo?.noIndex ?? true,
+    nofollow: searchSeo?.noFollow ?? true,
+    title,
+    description,
+    titleTemplate,
+    canonical,
+    openGraph: {
+      type: 'website',
+      title: title,
+      description: description,
+    },
+  }
+}
+
+function Page({
+  page: searchContentType,
+  globalSections: globalSectionsProp,
+  searchTerm,
+}: SearchPageProps) {
+  const { sections: globalSections, settings: globalSettings } =
+    globalSectionsProp ?? {}
+  const { settings } = searchContentType
+  const applySearchState = useApplySearchState()
+  const searchParams = useSearchParams({
+    sort: settings?.productGallery?.sortBySelection as SearchState['sort'],
+  })
+
+  const itemsPerPage = settings?.productGallery?.itemsPerPage ?? ITEMS_PER_PAGE
+
+  if (!searchParams) {
+    return null
+  }
+
+  const [effectiveSearchTerm, setEffectiveSearchTerm] = useState<
+    string | undefined
+  >(() => searchTerm ?? undefined)
+
+  useEffect(() => {
+    if (!searchTerm && searchParams.term) {
+      setEffectiveSearchTerm(searchParams.term)
+    }
+  }, [searchParams.term, searchTerm])
+
+  const { noindex, nofollow, ...seoData } = generateSEOData(
+    storeConfig,
+    effectiveSearchTerm,
+    settings?.seo
+  )
+
+  return (
+    <SearchProvider
+      onChange={applySearchState}
+      itemsPerPage={itemsPerPage}
+      shouldResetInfiniteScroll={!storeConfig.experimental?.scrollRestoration}
+      {...searchParams}
+    >
+      {/* SEO */}
+      <NextSeo noindex={noindex} nofollow={nofollow} {...seoData} />
+
+      <UISROnly text={seoData.title} />
+
+      {/*
+          WARNING: Do not import or render components from any
+          other folder than '../components/sections' in here.
+
+          This is necessary to keep the integration with the CMS
+          easy and consistent, enabling the change and reorder
+          of elements on this page.
+
+          If needed, wrap your component in a <Section /> component
+          (not the HTML tag) before rendering it here.
+        */}
+      <SearchWrapper
+        itemsPerPage={itemsPerPage}
+        searchContentType={searchContentType}
+        serverData={{
+          title: seoData.title,
+          searchTerm: effectiveSearchTerm,
+        }}
+        globalSections={globalSections}
+        globalSettings={globalSettings}
+      />
+    </SearchProvider>
+  )
+}
+
+export { getStaticProps }
+
+export default Page
diff --git a/node_modules/@faststore/core/src/sdk/analytics/platform/vtex/search.ts b/node_modules/@faststore/core/src/sdk/analytics/platform/vtex/search.ts
index 9f8ae79..453f65b 100644
--- a/node_modules/@faststore/core/src/sdk/analytics/platform/vtex/search.ts
+++ b/node_modules/@faststore/core/src/sdk/analytics/platform/vtex/search.ts
@@ -109,7 +109,7 @@ const sendEvent = (options: SearchEvent & { url?: string }) => {
     headers: {
       'content-type': 'application/json',
     },
-  })
+  }).catch(() => {})
 }
 
 const isFullTextSearch = (url: URL) =>
